diff --git content/public/common/common_param_traits_macros.h content/public/common/common_param_traits_macros.h
index 54b13c9a587b..2a58399679f9 100644
--- content/public/common/common_param_traits_macros.h
+++ content/public/common/common_param_traits_macros.h
@@ -203,6 +203,7 @@ IPC_STRUCT_TRAITS_BEGIN(content::WebPreferences)
   IPC_STRUCT_TRAITS_MEMBER(text_autosizing_enabled)
   IPC_STRUCT_TRAITS_MEMBER(double_tap_to_zoom_enabled)
   IPC_STRUCT_TRAITS_MEMBER(web_app_scope)
+  IPC_STRUCT_TRAITS_MEMBER(caret_width)
 #if defined(OS_ANDROID)
   IPC_STRUCT_TRAITS_MEMBER(font_scale_factor)
   IPC_STRUCT_TRAITS_MEMBER(device_scale_adjustment)
diff --git content/public/common/web_preferences.h content/public/common/web_preferences.h
index a508264986d8..273fb524e15d 100644
--- content/public/common/web_preferences.h
+++ content/public/common/web_preferences.h
@@ -225,6 +225,8 @@ struct CONTENT_EXPORT WebPreferences {
 
   bool double_tap_to_zoom_enabled;
 
+  int caret_width;
+
   bool text_autosizing_enabled;
 
   // Representation of the Web App Manifest scope if any.
diff --git content/renderer/render_view_impl.cc content/renderer/render_view_impl.cc
index e816955e4c60..8bac1813b6a3 100644
--- content/renderer/render_view_impl.cc
+++ content/renderer/render_view_impl.cc
@@ -818,6 +818,7 @@ void RenderView::ApplyWebPreferences(const WebPreferences& prefs,
 
   settings->SetTextAutosizingEnabled(prefs.text_autosizing_enabled);
   settings->SetDoubleTapToZoomEnabled(prefs.double_tap_to_zoom_enabled);
+  settings->SetCaretWidth(prefs.caret_width);
   blink::WebNetworkStateNotifier::SetNetworkQualityWebHoldback(
       static_cast<blink::WebEffectiveConnectionType>(
           prefs.network_quality_estimator_web_holdback));
diff --git third_party/blink/public/web/web_settings.h third_party/blink/public/web/web_settings.h
index 2856333395a3..952519adb42d 100644
--- third_party/blink/public/web/web_settings.h
+++ third_party/blink/public/web/web_settings.h
@@ -134,6 +134,7 @@ class WebSettings {
   virtual void SetAntialiasedClips2dCanvasEnabled(bool) = 0;
   virtual void SetAutoplayPolicy(AutoplayPolicy) = 0;
   virtual void SetAutoZoomFocusedNodeToLegibleScale(bool) = 0;
+  virtual void SetCaretWidth(int) = 0;
   virtual void SetClobberUserAgentInitialScaleQuirk(bool) = 0;
   virtual void SetCookieEnabled(bool) = 0;
   virtual void SetNavigateOnDragDrop(bool) = 0;
diff --git third_party/blink/renderer/core/exported/web_settings_impl.cc third_party/blink/renderer/core/exported/web_settings_impl.cc
index 9eee26ccff38..182ded59b588 100644
--- third_party/blink/renderer/core/exported/web_settings_impl.cc
+++ third_party/blink/renderer/core/exported/web_settings_impl.cc
@@ -149,6 +149,10 @@ void WebSettingsImpl::SetAutoZoomFocusedNodeToLegibleScale(
       auto_zoom_focused_node_to_legible_scale;
 }
 
+void WebSettingsImpl::SetCaretWidth(int width) {
+  settings_->SetCaretWidth(width);
+}
+
 void WebSettingsImpl::SetTextAutosizingEnabled(bool enabled) {
   dev_tools_emulator_->SetTextAutosizingEnabled(enabled);
 }
diff --git third_party/blink/renderer/core/exported/web_settings_impl.h third_party/blink/renderer/core/exported/web_settings_impl.h
index ebe2619f53de..b9d03f106d9a 100644
--- third_party/blink/renderer/core/exported/web_settings_impl.h
+++ third_party/blink/renderer/core/exported/web_settings_impl.h
@@ -64,6 +64,7 @@ class CORE_EXPORT WebSettingsImpl final : public WebSettings {
   void SetAntialiased2dCanvasEnabled(bool) override;
   void SetAntialiasedClips2dCanvasEnabled(bool) override;
   void SetAutoZoomFocusedNodeToLegibleScale(bool) override;
+  void SetCaretWidth(int) override;
   void SetClobberUserAgentInitialScaleQuirk(bool) override;
   void SetCookieEnabled(bool) override;
   void SetNavigateOnDragDrop(bool) override;
diff --git third_party/blink/renderer/core/frame/local_frame_view.cc third_party/blink/renderer/core/frame/local_frame_view.cc
index 981cb1e0f685..78cff93663b6 100644
--- third_party/blink/renderer/core/frame/local_frame_view.cc
+++ third_party/blink/renderer/core/frame/local_frame_view.cc
@@ -4249,8 +4249,11 @@ void LocalFrameView::ApplyTransformForTopFrameSpace(
 }
 
 LayoutUnit LocalFrameView::CaretWidth() const {
+  int width = 1;
+  if (frame_->GetSettings())
+    width = GetPage()->GetSettings().GetCaretWidth();
   return LayoutUnit(
-      std::max<float>(1.0, GetChromeClient()->WindowToViewportScalar(1)));
+      std::max<float>(1.0, GetChromeClient()->WindowToViewportScalar(width)));
 }
 
 LocalFrameUkmAggregator& LocalFrameView::EnsureUkmAggregator() {
diff --git third_party/blink/renderer/core/frame/settings.json5 third_party/blink/renderer/core/frame/settings.json5
index 8bfab5fd269b..2c1fb8f0761d 100644
--- third_party/blink/renderer/core/frame/settings.json5
+++ third_party/blink/renderer/core/frame/settings.json5
@@ -1012,6 +1012,11 @@
       initial: 800,
       type: "int",
     },
+	{
+      name: "caretWidth",
+      initial: 1,
+      type: "int",
+    },
 
     // Preferred color scheme from the OS/application passed to the renderer for
     // evaluating the prefers-color-scheme media query.
